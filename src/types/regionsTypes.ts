export interface IRegion {
    id: number,
    region_code: number | string,
    region_id: number | string,
    psgc_code: number | string,
    region_name: string,
    createdAt: Date,
    updatedAt: Date,
}